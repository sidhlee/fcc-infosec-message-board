const getThreads = () => {};

const createThread = () => {};

const reportThread = () => {};

const deleteThread = () => {};

module.exports = {
  getThreads,
  createThread,
  reportThread,
  deleteThread,
};
